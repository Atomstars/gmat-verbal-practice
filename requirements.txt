# Core runtime dependencies for all three parsers + vector search API.
# Install:  pip install -r requirements.txt

# PDF parsing (Verbal + Quant)
pdfplumber==0.11.9
PyMuPDF==1.27.2.3

# EPUB parsing (Verbal)
beautifulsoup4==4.15.0
lxml==6.1.1

# Diagram extraction (Quant)
Pillow==12.2.0

# Embeddings (all parsers + api.py)
sentence-transformers==5.6.0

# Vector search API
qdrant-client==1.18.0
fastapi==0.138.0
uvicorn==0.49.0

# Schema validation (parsers)
pydantic==2.13.4
